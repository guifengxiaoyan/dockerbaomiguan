services:
  autobaomiguan:
    build: .
    image: autobaomiguan:latest
    stdin_open: true
    tty: true
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
    dns:
      - 223.5.5.5
      - 8.8.8.8
    restart: unless-stopped
